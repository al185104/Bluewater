<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Bluewater.App.Views.FatalErrorPage">
  <ScrollView>
    <Grid Padding="32">
      <VerticalStackLayout
        Spacing="20"
        MaximumWidthRequest="520"
        HorizontalOptions="Center"
        VerticalOptions="Center">
        <Label
          Text="A critical error occurred"
          FontSize="28"
          FontAttributes="Bold"
          HorizontalTextAlignment="Center" />
        <Label
          Text="{Binding Message}"
          FontSize="16"
          HorizontalTextAlignment="Center"
          LineBreakMode="WordWrap" />
        <Label
          Text="The application has been stopped to prevent inconsistent behavior. Please restart the app and share the generated diagnostic log with the administrator if the problem continues."
          FontSize="14"
          HorizontalTextAlignment="Center"
          LineBreakMode="WordWrap" />
      </VerticalStackLayout>
    </Grid>
  </ScrollView>
</ContentPage>
