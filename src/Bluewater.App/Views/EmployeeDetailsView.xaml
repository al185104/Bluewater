<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Bluewater.App.Models"
             xmlns:controls="clr-namespace:Bluewater.App.Views.Controls"
             x:Class="Bluewater.App.Views.EmployeeDetailsView"
             x:Name="RootView">
    <controls:SlideInEditorView x:Name="EditorView"
                                Title="{Binding Source={x:Reference RootView}, Path=Title}"
                                IsOpen="{Binding Source={x:Reference RootView}, Path=IsOpen}"
                                UpdateCommand="{Binding Source={x:Reference RootView}, Path=UpdateCommand}"
                                UpdateCommandParameter="{Binding Source={x:Reference RootView}, Path=UpdateCommandParameter}"
                                UpdateButtonText="{Binding Source={x:Reference RootView}, Path=PrimaryButtonText}"
                                CloseCommand="{Binding Source={x:Reference RootView}, Path=CloseCommand}"
                                CloseCommandParameter="{Binding Source={x:Reference RootView}, Path=CloseCommandParameter}">
        <controls:SlideInEditorView.EditorContent>
            <Grid>
                <VerticalStackLayout x:Name="EmptyStateLayout"
                                     Spacing="8"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center"
                                     IsVisible="False">
                    <Label Text="Select an employee to edit or create a new employee."
                           HorizontalTextAlignment="Center"
                           Style="{StaticResource BodyTextStyle}" />
                    <Label Text="Use the actions on the employee list to begin editing or add a new employee."
                           HorizontalTextAlignment="Center"
                           FontSize="12"
                           TextColor="{AppThemeBinding Light=#6B7280, Dark=#9CA3AF}" />
                </VerticalStackLayout>
                <ScrollView x:Name="FormScrollView"
                            IsVisible="False">
                    <VerticalStackLayout x:Name="FormLayout"
                                         Spacing="20"
                                         Padding="0,0,12,24"
                                         x:DataType="models:EditableEmployee">
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Personal Information"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="First name"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding FirstName}"
                                           Placeholder="First name"
                                           Keyboard="Default" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Last name"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding LastName}"
                                           Placeholder="Last name"
                                           Keyboard="Default" />
                                </VerticalStackLayout>
                            </Grid>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Middle name"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding MiddleName}"
                                       Placeholder="Middle name"
                                       Keyboard="Default" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Date of birth"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <DatePicker Date="{Binding DateOfBirth, Mode=TwoWay}" />
                            </VerticalStackLayout>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Gender"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Picker ItemsSource="{Binding GenderOptions}"
                                            SelectedItem="{Binding Gender}"
                                            Title="Select gender" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Civil status"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Picker ItemsSource="{Binding CivilStatusOptions}"
                                            SelectedItem="{Binding CivilStatus}"
                                            Title="Select civil status" />
                                </VerticalStackLayout>
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Blood type"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Picker ItemsSource="{Binding BloodTypeOptions}"
                                            SelectedItem="{Binding BloodType}"
                                            Title="Select blood type" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Status"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Picker ItemsSource="{Binding StatusOptions}"
                                            SelectedItem="{Binding Status}"
                                            Title="Select status" />
                                </VerticalStackLayout>
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Height (cm)"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding Height}"
                                           Placeholder="Height"
                                           Keyboard="Numeric" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Weight (kg)"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding Weight}"
                                           Placeholder="Weight"
                                           Keyboard="Numeric" />
                                </VerticalStackLayout>
                            </Grid>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Tenant"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Picker ItemsSource="{Binding TenantOptions}"
                                        SelectedItem="{Binding Tenant}"
                                        Title="Select tenant" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Meal credits"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding MealCredits}"
                                       Placeholder="Meal credits"
                                       Keyboard="Numeric" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Remarks"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Editor Text="{Binding Remarks}"
                                        Placeholder="Additional remarks" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Job Details"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <Picker x:Name="PositionPicker"
                                        ItemsSource="{Binding Source={x:Reference RootView}, Path=Positions}"
                                        ItemDisplayBinding="{Binding Name}"
                                        HeightRequest="70"
                                        Title="Position"
                                        SelectedIndexChanged="OnPositionPickerSelectedIndexChanged" />
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Department"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding Department}"
                                           Placeholder="Department" />
                                </VerticalStackLayout>
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Section"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding Section}"
                                           Placeholder="Section" />
                                </VerticalStackLayout>
                                <Picker x:Name="TypePicker"
                                        Grid.Column="1"
                                        ItemsSource="{Binding Source={x:Reference RootView}, Path=EmployeeTypes}"
                                        ItemDisplayBinding="{Binding Name}"
                                        Title="Type"
                                        SelectedIndexChanged="OnTypePickerSelectedIndexChanged" />
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <Picker x:Name="LevelPicker"
                                        ItemsSource="{Binding Source={x:Reference RootView}, Path=Levels}"
                                        ItemDisplayBinding="{Binding Name}"
                                        Title="Level"
                                        SelectedIndexChanged="OnLevelPickerSelectedIndexChanged" />
                                <Picker x:Name="ChargingPicker"
                                        Grid.Column="1"
                                        ItemsSource="{Binding Source={x:Reference RootView}, Path=Chargings}"
                                        ItemDisplayBinding="{Binding Name}"
                                        Title="Select charging"
                                        SelectedIndexChanged="OnChargingPickerSelectedIndexChanged" />
                            </Grid>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Image URL"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding Image}"
                                       Placeholder="Image URL" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Contact Information"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Email"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding Email}"
                                       Placeholder="Email address"
                                       Keyboard="Email" />
                            </VerticalStackLayout>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Telephone"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding TelNumber}"
                                           Placeholder="Telephone number" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Mobile"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding MobileNumber}"
                                           Placeholder="Mobile number" />
                                </VerticalStackLayout>
                            </Grid>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Address"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding Address}"
                                       Placeholder="Current address" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Provincial address"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding ProvincialAddress}"
                                       Placeholder="Provincial address" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Mother's maiden name"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding MothersMaidenName}"
                                       Placeholder="Mother's maiden name" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Father's name"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding FathersName}"
                                       Placeholder="Father's name" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Emergency Contact"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Contact name"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding EmergencyContact}"
                                       Placeholder="Emergency contact name" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Relationship"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding RelationshipContact}"
                                       Placeholder="Relationship" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Contact address"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding AddressContact}"
                                       Placeholder="Contact address" />
                            </VerticalStackLayout>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Contact telephone"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding TelNoContact}"
                                           Placeholder="Telephone" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Contact mobile"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding MobileNoContact}"
                                           Placeholder="Mobile" />
                                </VerticalStackLayout>
                            </Grid>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Education"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Educational attainment"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Picker ItemsSource="{Binding EducationalAttainmentOptions}"
                                        SelectedItem="{Binding EducationalAttainment}"
                                        Title="Select attainment" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Course graduated"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding CourseGraduated}"
                                       Placeholder="Course" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="University"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding UniversityGraduated}"
                                       Placeholder="University" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Employment"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Date hired"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <DatePicker Date="{Binding DateHired, Mode=TwoWay}" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Date regularized"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <DatePicker Date="{Binding DateRegularized, Mode=TwoWay}" />
                                </VerticalStackLayout>
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Date resigned"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <DatePicker Date="{Binding DateResigned, Mode=TwoWay}" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Date terminated"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <DatePicker Date="{Binding DateTerminated, Mode=TwoWay}" />
                                </VerticalStackLayout>
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="TIN"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding TinNo}"
                                           Placeholder="TIN number" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="SSS"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding SssNo}"
                                           Placeholder="SSS number" />
                                </VerticalStackLayout>
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="HDMF"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding HdmfNo}"
                                           Placeholder="HDMF number" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="PHIC"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding PhicNo}"
                                           Placeholder="PHIC number" />
                                </VerticalStackLayout>
                            </Grid>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Bank account"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding BankAccount}"
                                       Placeholder="Bank account" />
                            </VerticalStackLayout>
                            <HorizontalStackLayout Spacing="12"
                                                   VerticalOptions="Center">
                                <Label Text="Has service charge"
                                       VerticalOptions="Center"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Switch IsToggled="{Binding HasServiceCharge}" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="User Account"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Username"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding Username}"
                                       Placeholder="Username"
                                       Keyboard="Default" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Password hash"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding PasswordHash}"
                                       Placeholder="Password hash"
                                       Keyboard="Default" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Credential"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Picker ItemsSource="{Binding CredentialOptions}"
                                        SelectedItem="{Binding Credential}"
                                        Title="Select credential" />
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Supervised group"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding SupervisedGroup}"
                                       Placeholder="Supervised group ID"
                                       Keyboard="Default" />
                            </VerticalStackLayout>
                            <HorizontalStackLayout Spacing="12"
                                                   VerticalOptions="Center">
                                <Label Text="Is global supervisor"
                                       VerticalOptions="Center"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Switch IsToggled="{Binding IsGlobalSupervisor}" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="12">
                            <Label Text="Pay Details"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyTextStyle}" />
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Basic pay"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding BasicPay}"
                                           Placeholder="Basic pay"
                                           Keyboard="Numeric" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="Daily rate"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding DailyRate}"
                                           Placeholder="Daily rate"
                                           Keyboard="Numeric" />
                                </VerticalStackLayout>
                            </Grid>
                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="Hourly rate"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding HourlyRate}"
                                           Placeholder="Hourly rate"
                                           Keyboard="Numeric" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="4">
                                    <Label Text="HDMF contribution"
                                           Style="{StaticResource CaptionTextStyle}" />
                                    <Entry Text="{Binding HdmfCon}"
                                           Placeholder="HDMF contribution"
                                           Keyboard="Numeric" />
                                </VerticalStackLayout>
                            </Grid>
                            <VerticalStackLayout Spacing="4">
                                <Label Text="HDMF employer share"
                                       Style="{StaticResource CaptionTextStyle}" />
                                <Entry Text="{Binding HdmfEr}"
                                       Placeholder="HDMF employer share"
                                       Keyboard="Numeric" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </controls:SlideInEditorView.EditorContent>
    </controls:SlideInEditorView>
</ContentView>
