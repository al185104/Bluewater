<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Bluewater.App.Models"
             xmlns:selectors="clr-namespace:Bluewater.App.Selectors"
             x:Class="Bluewater.App.Views.ShiftsPage"
             Title="Shifts">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="ShiftRowBorderStyle"
                   TargetType="Border">
                <Setter Property="StrokeThickness"
                        Value="1" />
                <Setter Property="Stroke"
                        Value="Transparent" />
                <Setter Property="Padding"
                        Value="16" />
            </Style>
            <Style x:Key="ShiftEntryStyle"
                   TargetType="Entry">
                <Setter Property="BackgroundColor"
                        Value="Transparent" />
                <Setter Property="TextColor"
                        Value="{AppThemeBinding Light=#444444, Dark=#DDDDDD}" />
                <Setter Property="Margin"
                        Value="0" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="VerticalOptions"
                        Value="Center" />
            </Style>
            <Style x:Key="ShiftTimePickerStyle"
                   TargetType="TimePicker">
                <Setter Property="BackgroundColor"
                        Value="Transparent" />
                <Setter Property="TextColor"
                        Value="{AppThemeBinding Light=#444444, Dark=#DDDDDD}" />
                <Setter Property="Format"
                        Value="HH:mm" />
                <Setter Property="Margin"
                        Value="0" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="HorizontalOptions"
                        Value="Fill" />
                <Setter Property="VerticalOptions"
                        Value="Fill" />
                <Setter Property="FontSize"
                        Value="16" />
            </Style>
            <DataTemplate x:Key="ShiftPrimaryTemplate"
                          x:DataType="models:ShiftSummary">
                <Border Style="{StaticResource ShiftRowBorderStyle}">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor"
                                            Value="{AppThemeBinding Light=#FFFFFF, Dark=#1F1F1F}" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor"
                                            Value="#0A386EB3" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    <Grid ColumnSpacing="12"
                          ColumnDefinitions="*,*,*,*,*,0.5*"
                          VerticalOptions="Center">
                        <Entry Text="{Binding Name}"
                               Style="{StaticResource ShiftEntryStyle}"
                               FontAttributes="Bold"
                               Placeholder="Shift name" />
                        <TimePicker Grid.Column="1"
                                    Time="{Binding ShiftStartTimeValue}"
                                    Style="{StaticResource ShiftTimePickerStyle}" />
                        <TimePicker Grid.Column="2"
                                    Time="{Binding ShiftBreakTimeValue}"
                                    Style="{StaticResource ShiftTimePickerStyle}" />
                        <TimePicker Grid.Column="3"
                                    Time="{Binding ShiftBreakEndTimeValue}"
                                    Style="{StaticResource ShiftTimePickerStyle}" />
                        <TimePicker Grid.Column="4"
                                    Time="{Binding ShiftEndTimeValue}"
                                    Style="{StaticResource ShiftTimePickerStyle}" />
                        <Label Grid.Column="5"
                               Text="{Binding BreakHoursDisplay}"
                               HorizontalTextAlignment="Center"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light=#444444, Dark=#DDDDDD}" />
                    </Grid>
                </Border>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid>
        <Grid Padding="20,40"
              RowDefinitions="Auto,*"
              RowSpacing="16">
            <Grid ColumnDefinitions="*,Auto"
                  RowDefinitions="Auto,Auto"
                  RowSpacing="6">
                <Label Text="Shifts"
                       Style="{StaticResource HeadingTextStyle}" />
                <HorizontalStackLayout Grid.Column="1"
                                       Grid.Row="0"
                                       Spacing="10"
                                       HorizontalOptions="End"
                                       VerticalOptions="Center">
                    <Button Text="Import"
                            Command="{Binding ImportShiftsCommand}"
                            SemanticProperties.Hint="Import shifts from a CSV file" />
                    <Button Text="Add"
                            Command="{Binding AddShiftCommand}" />
                    <Button Text="Save"
                            Command="{Binding SaveShiftCommand}"
                            IsEnabled="{Binding CanSaveSelectedShift}" />
                    <Button Text="Update"
                            Command="{Binding UpdateShiftCommand}"
                            IsEnabled="{Binding CanUpdateSelectedShift}" />
                    <Button Text="Delete"
                            Command="{Binding DeleteShiftCommand}"
                            IsEnabled="{Binding CanDeleteSelectedShift}" />
                </HorizontalStackLayout>
                <Label Grid.Row="1"
                       Grid.ColumnSpan="2"
                       Text="{Binding ImportStatusMessage}"
                       Style="{StaticResource BodyTextStyle}"
                       TextColor="{AppThemeBinding Light=#0A386E, Dark=#9CC4FF}"
                       IsVisible="{Binding HasImportStatusMessage}" />
            </Grid>
            <Grid Grid.Row="1">
                <CollectionView ItemsSource="{Binding Shifts}"
                                ItemTemplate="{StaticResource ShiftPrimaryTemplate}"
                                SelectionMode="Single"
                                SelectedItem="{Binding SelectedShift}"
                                VerticalOptions="Fill">
                    <CollectionView.Header>
                        <Border StrokeShape="RoundRectangle 10 10 0 0"
                                BackgroundColor="{AppThemeBinding Light={StaticResource AccentColor}, Dark=#1F5AA8}">
                            <Grid ColumnSpacing="12"
                                  Padding="20,10"
                                  ColumnDefinitions="*,*,*,*,*,0.5*">
                                <Label Text="Name"
                                       TextColor="White"
                                       FontAttributes="Bold" />
                                <Label Grid.Column="1"
                                       Text="Start"
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="Center" />
                                <Label Grid.Column="2"
                                       Text="Break Start"
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="Center" />
                                <Label Grid.Column="3"
                                       Text="Break End"
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="Center" />
                                <Label Grid.Column="4"
                                       Text="End"
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="Center" />
                                <Label Grid.Column="5"
                                       Text="Break Hours"
                                       TextColor="White"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="Center" />
                            </Grid>
                        </Border>
                    </CollectionView.Header>
                    <CollectionView.EmptyView>
                        <VerticalStackLayout HorizontalOptions="Center"
                                             VerticalOptions="Center"
                                             Spacing="8">
                            <Label Text="No shifts found"
                                   HorizontalTextAlignment="Center"
                                   FontAttributes="Bold" />
                            <Label Text="Shifts will appear here once they are available."
                                   FontSize="12"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>
                <ActivityIndicator IsRunning="{Binding IsBusy}"
                                   IsVisible="{Binding IsBusy}"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
            </Grid>
        </Grid>
    </Grid>
</ContentPage>
